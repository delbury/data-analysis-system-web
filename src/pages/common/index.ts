import * as maps from './format-maps';
import * as opts from './select-options';
import * as remote from './remote-option';
import * as formatters from './formatters';

export default {
  maps,
  opts,
  remote,
  formatters,
};
